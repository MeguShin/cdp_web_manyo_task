<h1><%= t('.Show') %></h1>

<p>
    <strong><%= User.human_attribute_name(:name) %>:</strong>
    <%= @user.name %>
</p>
<p>
    <strong><%= User.human_attribute_name(:email) %>:</strong>
    <%= @user.email %>
</p>

<%= link_to "編集", edit_user_path(@user), id: "edit-user" %><br>
<%= link_to "削除", user_path(@user), method: :delete, data: { confirm: t('common.confirm_destroy') } %></td>
