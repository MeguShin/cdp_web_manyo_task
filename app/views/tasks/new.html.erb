<div class="container mt-4">
    <h1><%= t('.New Task') %></h1>

    <%= render 'form', task: @task %><br>

    <p><%= link_to t('common.show'), @task, id: "create-task", class: 'btn btn-success' %></p>
    <p><%= link_to t('common.back'), tasks_path, id: "back", class: 'btn btn-secondary' %></p>
</div>
